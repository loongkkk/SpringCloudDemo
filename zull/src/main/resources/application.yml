server:
  port: 8101
spring:
  application:
    name: zuul
eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8088/eureka/
management:
  endpoints:
    web:
      exposure:
        include: '*'